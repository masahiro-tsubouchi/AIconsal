"""Agents package

Place one agent per file (e.g., manufacturing_advisor.py, python_mentor.py)
Each agent should expose a clear entry point (e.g., `run(state)` or similar).
"""
from __future__ import annotations
